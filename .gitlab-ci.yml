image: macodigo/hugo-nodejs-macodigo

variables:
  GIT_SUBMODULE_STRATEGY: recursive

before_script:
  - npm install

pages:
  script:
  - hugo
  artifacts:
    paths:
    - public
  only:
  - master

